---
name: tv-indicator-to-strategy
description: Use when a user provides a TradingView indicator, Pine source, indicator name, chart screenshot, or natural-language trading idea and wants a source audit, Strategy Spec, Pine strategy, real TradingView backtest, bounded parameter research, or an HTML report.
---

# TradingView 指标转策略

把用户感兴趣的指标研究清楚，转换成可审阅的 Pine strategy，再连接用户本机的 TradingView Desktop 获取真实 Strategy Tester 证据。所有收益、回撤、PF、交易数和截图都必须来自当前可审计运行，不能根据指标说明或本地推算编造。

执行前将本文件所在目录解析为绝对路径 `SKILL_DIR`，并设置：

```bash
ENGINE="$SKILL_DIR/engine/auto_inject.mjs"
```

不同 Agent 的安装目录不同，不要假定某个产品专属环境变量一定存在。

## 用户可以提供什么

| 输入 | 处理方式 |
| --- | --- |
| Pine 源码 | 核对身份后直接审计与转换 |
| TradingView 链接 | 核对名称、作者、说明和公开源码状态 |
| 指标名称与作者 | 用 `engine/resolve_indicator.mjs` 查找准确实现；同名时先消歧 |
| 图表截图 | 只解释可观察行为，并列出无法从截图确认的计算与时序 |
| 自然语言或多指标想法 | 先形成 Strategy Spec，审阅通过后再生成 Pine |

无论入口是什么，都先固定研究对象的名称、作者或来源、源码版本、品种、周期和参数。找不到源码时可以继续做概念研究，但不得声称完成了源码审计或真实回测。

## 标准工作流

### 1. 解释指标并审计源码

先让用户真正理解指标：它测量什么、信号怎样形成、何时确认、何时失效、关键参数改变什么、适用与失效环境是什么。详细清单见 [analysis.md](analysis.md)。

同时检查未来数据和重绘风险，至少覆盖：

- `lookahead_on`、负 offset 和把未来确认结果画回过去；
- 未收盘 K 线、`calc_on_every_tick` 和跨周期数据；
- pivot 确认延迟；
- 条件分支中的滚动函数导致的陈旧序列；
- 图形标记与真实可交易时点的差异。

把每项结论标为源码事实、正式资料、图形观察、数学推论或研究假设。证据不足时明确说明，不猜实现。

### 2. 形成 Strategy Spec

在写 Pine 或连接 TradingView 前，明确：

- 市场与周期；
- 多空方向；
- 入场、过滤器和退出；
- 仓位与是否加仓；
- 收盘确认或其他执行时点；
- 手续费、滑点和明确不做的规则。

自然语言或多指标输入使用 `engine/strategy_spec_cli.mjs` 依次执行 `validate`、`compile --out` 和 `inspect --spec`。`validate` 返回阻塞歧义时，只合并询问必要问题；不能静默补规则。完整约定见 [strategy-spec.md](strategy-spec.md)，指标转策略规则见 [conversion.md](conversion.md)。

### 3. 建立真实回测基线

先按 [公开版运行手册](public-runbook.md) 运行探针，确认 TradingView Desktop、CDP、图表、Pine Editor 和 Strategy Tester 状态。首次安装在 `engine/` 运行 `bash install.sh`；环境设置见 [SETUP.md](SETUP.md)。

若本机保存了不含其他研究指标的专用图表布局，回测命令附加 `--chart-layout "<精确布局名>"`。布局属于用户设备，Skill 不预设名称；指定后必须精确命中并在注入前切换，找不到或存在未保存修改时就停止，不自动舍弃用户工作。没有专用布局时不删除用户内容，但 proof 截图前必须确认画面只包含目标策略、Strategy Tester 和必要价格图。

基线必须记录同一次运行的：源码身份、策略身份、symbol、timeframe、评估区间、成本、参数、策略收益、买入并持有、最大回撤、PF、交易数和 proof 状态。输入变化需要重新注入时，不得沿用旧结果。

### 4. 运行标准有界参数研究

研究范围按 TradingView 执行预算决定，不按 Agent 工作量贴“轻度”或“深化”标签：

- 只需解释或验证转换时，完成一组可解释基线。
- 用户要求研究参数、寻找更优配置或完整验证时，直接运行标准有界搜索，不再询问选择哪种深度。
- 先枚举全部指标输入，并将其标为扫描、固定、派生、执行/成本或仅视觉参数。优先扫描 1–3 个真正改变信号或风险的参数。
- 合理网格不超过 30 组时完整覆盖；更大网格默认以 30 次完整回测做均匀粗搜和有效 Top-1 邻域复核。用户可以用 `--budget` 明确提高或降低执行预算。
- 搜索包含合理宽范围、边界或哨兵组合；只有参数面有效时才继续检查邻域。
- 报告明确写出搜索维数和实际格数。两轴显示完整参数矩阵；三轴及以上只显示 Top-1 所在、且具有真实二维邻域的切片，不能把切片写成完整参数面。

结果只能称为“本次有界搜索 Top-1”。孤立峰、零交易、死参数、样本过低、基准不可比较或全部候选无有效优势时停止扩参，转为机制验证，不用更多网格包装失败结果。具体目标函数、参数高原和停止条件见 [tuning.md](tuning.md)。

### 5. 用固定规则做三市场复核

对外完整案例使用同一源码、参数、周期和成本复核 QQQ、SPY、510300。开发市场可以先运行，但最终不能逐市场换参后再合并成“跨市场稳健”。

只有区间口径可比时才按相对买入并持有的超额收益排序。基准漂移或加载区间不同应标为不可比较，并改用引擎给出的有效目标；不能继续把该结果解释成 alpha winner。

### 6. 恢复 Top-1 并生成报告

参数扫描结束后，把图表恢复到开发市场和本次 Top-1 参数，重新读取 Strategy Tester 并保存 proof。报告中的最终参数、市场和数字必须与恢复后的页面状态一致。

HTML 报告至少包含：

- 指标讲解与 Strategy Spec；
- 源码和重绘审计；
- 回测成本与评估区间；
- 搜索轴、范围、运行数、未覆盖参数和停止原因；
- QQQ、SPY、510300 的同口径结果；
- 本次相对最好的标准市场、策略收益、B&H、回撤、PF 和交易数；
- 限制，以及一个真正可执行的下一项实验或“研究决策”。

“停止扩参”属于研究决策，不应伪装成下一步。报告命令和数据字段见 [引擎 CLI](engine-cli.md)。
来源哈希和抓取方式应保留在运行证据中，但不要求固定占用报告正文；默认只保留紧凑的策略源码折叠入口，需要审计附录时才显式展示来源说明。

## 失败处理

同一自动化动作最多重试一次。随后根据 `run_summary.json` 的失败分类和 `nextRead` 读取最小必要证据。CDP、源码定位、编译、零交易、基准漂移、身份不一致和页面结构变化的处理见 [公开版排错指南](public-debugging.md)。

复杂脚本若连续超时，按运行手册的降级顺序保留原始交易语义并减少视觉部分；关键逻辑无法等价实现时停止，不能拿简化策略的结果冒充原指标。

## 诚实边界

- 没有真实 Strategy Tester 证据时，不输出收益、胜率、PF、回撤或最优参数。
- 单市场结果不能称为跨市场稳健；逐市场换参不能合并成共享参数结论。
- 正收益不等于跑赢买入并持有；过少交易和异常 PF 要显式提示样本风险。
- 不自动下单，不修改告警，不公开用户源码、账户状态、Cookie、令牌或本机路径。
- 公共包不含历史研究参数或私有案例；每次都从用户当前材料和当前 TradingView 证据开始。
