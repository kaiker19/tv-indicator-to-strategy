# 公开版排错指南

本指南只处理当前 TradingView 会话和当前指标，不依赖任何项目内部研究数据。

## 先收集最小证据

优先保留以下信息：

- `tv_probe.json`；
- 当前命令生成的 `run_summary.json`；
- 当前品种、周期、指标名称和源码身份；
- 编译错误或 Strategy Tester 的可见状态。

同一动作最多自动重试一次。第二次仍失败时，按失败类型处理，不进行无上限点击或刷新。

## CDP 无法连接

1. 确认 TradingView Desktop 正在运行且 CDP 端口与命令一致。
2. 确认端口未被另一实例占用。
3. 重新运行 `tv_probe.mjs`。
4. 仍不可用时，停止自动回测并说明需要用户恢复 Desktop 会话。

研究指标原理本身不要求 CDP；只有写入 Pine、应用脚本和读取真实 Strategy Tester 时才需要连接。

## 找不到指标或源码

- 优先使用用户打开的指标页面、Pine Editor 中的源码或明确提供的公开链接。
- 核对名称、作者和源码版本，避免选中同名复刻。
- 公开源码无法取得时，可以继续做概念研究，但不得声称已经完成源码审计或真实回测。

## Pine Editor 写入或应用失败

1. 确认目标标签页和 Pine Editor 可见。
2. 再次核对即将写入的策略身份，不覆盖用户未授权的脚本。
3. 写入后等待编辑器稳定，再触发保存或应用。
4. 读取编译提示；有错误时修复源码，不通过重复点击掩盖错误。

## 策略已应用但没有交易

依次检查：

- 日期范围内是否有足够历史数据；
- 入场条件是否可能成立；
- 信号是否只存在于视觉对象而未转为序列；
- 多空方向、仓位和订单时点是否符合 Strategy Spec；
- 输入更新后是否需要重新注入或重新应用脚本；
- 图表品种和周期是否与报告目标一致。

零交易是有效研究结果。确认逻辑无误后停止扩参，并说明导致零交易的条件。

## 参数更新后结果没有变化

先确认参数确实写入当前脚本，并在 Strategy Tester 中读取到新值。某些脚本修改源码输入后需要重新编译和应用；只修改界面但未重新注入，不能视为一次新回测。

## 买入并持有基准漂移

若相同市场的基准收益在不同运行间明显变化，通常意味着加载区间、起始日期或历史数据范围不同。此时：

1. 将比较标为不可兼容；
2. 保留策略自身的风险指标作为诊断信息；
3. 统一时间口径后重新运行；
4. 不把不可兼容结果计入跨市场超额。

## 身份或最终证明不一致

报告前核对源码哈希、策略名称、图表品种、周期、最终参数和 Strategy Tester 数据。任何一项不一致，都应恢复到目标状态后重新读取；无法恢复时把报告标为证据不完整。

## 页面结构变化

TradingView 界面变化导致自动化定位失败时，先运行探针并保存可见状态。不要凭旧坐标继续操作。若当前公开引擎无法识别新结构，停止写入并输出最小复现信息。

完整流程见 [公开版运行手册](public-runbook.md)，命令说明见 [引擎 CLI](engine-cli.md)。
