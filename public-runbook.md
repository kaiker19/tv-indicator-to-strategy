# 公开版运行手册

本手册只描述当前用户提供的指标和当前 TradingView 会话。公开包不包含项目内的历史研究、私有案例、预设参数库或研究数据。

## 运行前检查

1. 使用 Node.js 18 或更高版本。
2. 启动已登录的 TradingView Desktop，并为自动化会话开放 CDP 端口。
3. 将本 Skill 安装到所用 Agent，再从 Skill 根目录定位引擎：

   ```bash
   SKILL_DIR="${SKILL_DIR:-$PWD}"
   ENGINE="$SKILL_DIR/engine"
   cd "$ENGINE"
   npm ci --ignore-scripts
   npm run check
   ```

不要把示例中的环境变量替换为系统保留变量，也不要把登录信息、Cookie 或令牌写入研究文件。

## 标准流程

### 1. 确认连接和图表状态

先运行探针，确认 CDP、图表、品种、周期、Pine Editor 和 Strategy Tester 是否可用：

```bash
node "$ENGINE/tv_probe.mjs" --out /tmp/tv_probe.json
```

探针失败时先按 [公开版排错指南](public-debugging.md) 处理，不要直接进入回测。

如设备已有干净的研究专用布局，在后续 `auto_inject.mjs` 命令中传 `--chart-layout "<精确布局名>"`。引擎会在注入前切换，并把实际布局、symbol 和周期写进 manifest；指定名称不存在或存在未保存修改时失败关闭，不自动舍弃用户工作。没有专用布局可以沿用当前图表，但不得自动删除用户的其他研究内容，最终 proof 需确认不含无关指标和隐私信息。

### 2. 锁定指标身份和源码

- 记录指标名称、作者、公开页面链接和源码身份。
- 有 Pine 源码时以源码为准；只有名称或截图时，先完成来源核对和原理研究。
- 无法确认来源时明确标为“来源待确认”，不得用同名脚本冒充原指标。
- 保存源码哈希或等价身份信息，确保研究、生成策略和最终报告指向同一版本。

### 3. 形成可执行的 Strategy Spec

先说明信号、重绘风险、入场、出场、仓位、执行时点、成本和明确不做的事项，再生成 Pine 策略。策略转换细节见 [指标转策略约定](conversion.md)，结构化字段见 [Strategy Spec](strategy-spec.md)。

### 4. 建立基线

先用一组可解释参数完成单次回测，确认：

- 策略能够编译并产生可解释交易；
- 手续费、滑点、时间范围、品种和周期已记录；
- 指标身份、策略身份、页面状态和回测证据一致；
- 买入并持有基准的统计口径与策略口径兼容。

### 5. 进行标准有界参数扫描

用户要求研究参数、寻找更优配置或完整验证时，在基线后直接进入有界扫描：

- 先列出全部用户输入，并逐项标记为扫描、固定、派生或仅视觉参数；
- 优先选择 1–3 个会改变信号或风险的轴；
- 先覆盖宽范围，再检查当前最优点附近的相邻值；
- 保留至少一个边界或哨兵组合，用来发现无交易、失效区或异常敏感性；
- 合理网格不超过 30 组时完整覆盖；更大网格默认用 30 次完整回测完成均匀粗搜和有效邻域复核；用户给出的 `--budget` 优先；
- 结果只能称为“本次参数空间 Top-1”，不得声称找到了全局最优；
- 若全部组合无超额、零交易、有效样本过低、基准漂移或脚本身份不一致，停止扩参并转为机制验证。
- 报告明确写出搜索维数和实际运行格数。两轴直接展示完整敏感性矩阵；三轴及以上只能固定 Top-1 的其余参数展示二维切片，并明确它不是完整 N 维参数面。切片没有二维邻域时不生成伪矩阵。

参数选择和停止条件详见 [调参约定](tuning.md)。

### 6. 跨标准市场复核

在同一策略定义、成本和时间口径下复核 QQQ、SPY、510300。若数据可得区间导致基准不可比，报告必须显式标出，不将其计入跨市场超额结论。

最终报告至少包含：

- 研究对象和源码身份；
- Strategy Spec；
- 搜索范围、运行数、停止原因和未覆盖参数；
- 每个市场的收益、买入并持有、最大回撤、Profit Factor、交易数和证据状态；
- 当前参数空间 Top-1 及恢复后的证明回测；
- 失败原因、限制和下一项可证伪实验。

### 7. 恢复并证明最终状态

扫描结束后必须把当前图表恢复到报告中的 Top-1 参数和目标市场，再读取一次 Strategy Tester，保存最终参数、源码身份、页面身份与回测结果。没有恢复证明时，不得把扫描中间值写成最终结论。

## 执行预算

执行预算限制的是 TradingView 完整回测次数和 CDP 稳定性，不是 Agent 推理量。默认规则是：小网格完整覆盖；大网格最多 30 次粗搜与邻域复核；用户明确设置 `--budget` 时按该预算执行。无论运行多少次，都只能报告本次参数空间的相对最好结果，不能声称全局最优。

## 运行摘要和继续执行

自动化命令会写出 `run_summary.json`。重新进入任务时，先读取其中的阶段、最后成功步骤、失败分类、产物路径和 `nextRead`，只补做缺失步骤。不要重复已经有充分证据的回测。

## 复杂指标的降级顺序

复杂脚本无法一次完整转换时，依次尝试：

1. 保留原始信号，仅移除视觉对象；
2. 固定不影响交易含义的展示参数；
3. 把复合逻辑拆成可验证的信号模块；
4. 若关键语义仍无法等价实现，停止自动回测，输出不可验证原因和最小补充材料。

不得用简化策略的结果冒充原指标结果。

## 结束条件

只有在源码身份、策略定义、市场口径、最终参数和证据状态都能相互对应时，才输出完成报告。临时脚本、截图和运行摘要应放在用户指定的输出目录或系统临时目录，不写回公开 Skill。

命令参数和输出格式见 [引擎 CLI](engine-cli.md)。
