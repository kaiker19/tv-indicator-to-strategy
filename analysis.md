# 指标深度解读（J8 的展开——产出有价值的内容，不是一句话）

> SKILL.md 的 J8「先解释指标」要做**深**：数学原理 + 逻辑 + 它赌什么 + 局限 + 失效。
> 读完真源码后按此分析；产出既讲给用户听，也直接填进 HTML 报告的 `explain` 字段。

## 报告结构：固定骨架，内容自适应

正文固定三块，并允许在确有理解门槛时增加一个紧凑的前置解释，避免把某个指标的概念做成所有指标都有的栏目：

0. **先看懂它（按需）**：若指标名称建立在另一个基础指标或术语上，先用 `explain.readerGuide[]` 从基础版讲起，再说明当前版本改了什么、策略实际如何使用。每项为 `{ label, text }`，使用指标自身的自然标题，不编号、不写成大卡片；普通读者无需预先知道 SuperTrend、ATR、核回归或聚类等术语。专有名词必须在第一次可见正文出现时就用白话解释，顺序是“普通概念 → 专业名称 → 它改变了什么”，不能先写进 `howItWorks` 或 Strategy Spec，再隔几屏才定义。带宽、倍数、阈值等抽象参数尽量给一个简短数值锚点。基础概念能在 `howItWorks` 一句话内说清时省略此块。

1. **Strategy Spec（必选）**：用 `strategySpec.observation` 写清观察对象和实际计算的中枢、边界或状态，再写入场、出场、仓位、执行时点、成本、明确不做。指标自带或策略新增的可选条件放入 `strategySpec.modifiers[]`，没有就不显示；不要单独创建“过滤器”模块。若策略确有多类入口或状态分流，在这里用紧凑条目说明“市场状态 → 入场来源 → 对应退出/优先级”，不要新增通用报告模块。
2. **信号形成（按需）**：只有状态切换、候选确认或多阶段触发无法从 Strategy Spec 直接看懂时，才用 `explain.flow[]` 写 3–6 步，每步是 `{ label, text }`。语义顺序为“观察 → 计算/形成候选 → 何时确认 → 触发 → 执行”。简单入场、出场不得换词复述。多入口策略必须让用户能分清本次交易由哪个入口触发，不能只罗列一组可能同时为真的信号名。
3. **如何使用（必选）**：只保留 edge、1–3 个关键参数、适用和失效场景。对应 `explain.edge`、`explain.keyParams[]`、`worksWhen`、`failsWhen`。

其余内容合并进一个默认折叠的 `explain.deep`：直觉、符号说明、计算示例、公式、推导和关键代码。只渲染真实存在的子项；没有有意义的数学推导时不要硬凑公式。新报告优先使用结构化 `deep.equations[]`，每式填写 `label`、`expression`、`symbols[]` 和可选 `note`；所有 `symbols[]` 必须在 `deep.variables[]` 中逐一定义，并同时提供 `deep.example` 的 3–6 个简化数值。报告校验会拒绝缺符号或缺数字示例的标准公式。

版面顺序为先看懂它（按需）→ Strategy Spec → 信号形成（按需）→ 如何使用 → 深入理解。外层面向普通读者并保持简短；公式、推导、完整参数影响、重绘审计和关键代码统一放进默认折叠的“深入理解”。只有状态切换、候选确认或多阶段触发无法从 Strategy Spec 直接看懂时才写 `explain.flow[]`；简单的入场、出场条件不再换一种说法重复一遍。`deep.example` 必须先定义例子中的价格、状态和时间顺序，再解释状态如何变化；不得直接从“第 N 根候选点”开始。执行确认和成交时点只在 Strategy Spec 精确说明，其他段落引用它即可，不重复口号式强调“收盘确认”。

外层各块只承担一种职责：`oneLiner` 概述输入、输出和测试规则；`readerGuide` 补读者缺少的基础概念；Strategy Spec 写精确规则；`flow` 只保留无法从规则直接看出的时间顺序或状态转换；“如何使用”写参数与场景。相邻两块如果只是换词复述同一事实，删掉较泛的一段。已有 `readerGuide` 且 `howItWorks` 没增加新信息时，可以省略 `howItWorks`；Strategy Spec 已清楚给出简单入场、出场顺序时省略 `flow`。

可见正文只写用户能理解的交易语义。`process_orders_on_close=true`、`pyramiding=0`、`calc_on_every_tick` 等实现标志放进 Pine 或关键代码，不直接塞进 Strategy Spec；应改写为“按收盘价执行”“不重复加仓”等自然语言。SMA、EMA、ATR、PF 等缩写第一次出现时写出中文含义，随后再使用缩写。

文案使用直接的肯定句。少写“这不是……而是……”“不只是……更是……”“只负责……”这类成对句式，也不要为了显得完整机械凑成三点。能用一句事实说清，就不要先设一个反例再转折。

如果指标属于一个可替换的数学家族，要区分“本次源码固定使用什么”和“理论上哪些部件可以替换”。例如核回归要说明核函数、距离和带宽分别做什么：换核函数会改变权重公式，必须重新回测，不能把当前高斯核结果推广到其他核。普通读者先看到当前实现，替代方案放进深入理解，不在正文堆术语。

报告证据图必须看得见指标最有辨识度的几何结构：通道要同时看到中线和上下轨，趋势指标要看到趋势线与翻转点，振荡器要看到阈值与越界。全历史视图如果把线条压成一条、交易标记遮住指标或混入其他 study，就不算合格 proof；改用仍能核对标的、周期和策略身份的近期窗口。参数排名正文先写搜索市场和周期，只展示参数、策略目标指标、PF 与交易数；B&H、证据源和审计降级继续保存在结构化数据中，不作为每行可见列。

### 新手理解门槛

正文写完后，先从第一句话开始做“术语前置检查”：每个首次出现的数学名词、基础指标和英文参数，读者能否在当句或紧接着的一项中知道它是什么、起什么作用。再做一次外层去重，只比较 `oneLiner`、`readerGuide`、Strategy Spec、`flow` 和“如何使用”，深入理解不参与去重。最后让一个不了解该指标的普通用户回答“基础版在做什么、这个版本改了什么”，并检查能否回答：观察什么、信号何时才确定、什么条件精确触发交易、候选状态何时失效、哪些条件默认不启用。前两个问题答不出时补写或重写 `readerGuide`；交易问题答不出时重写 `flow`，不要靠增加重复模块补救。

## 分析清单（逐项想透，按自适应结构输出）

1. **它在算什么（机制）** — 2-3 句白话：输入是什么、怎么一步步算出信号。给普通人也能懂。
   → 一句话可放 `explain.howItWorks`；完整过程拆进 `explain.flow[]`

2. **数学原理（写出真公式）** — 把核心计算用清晰记号写出来，别只说"用了均线"。
   - 递归/平滑类写递推式（如 `SMMA_t = (SMMA_{t-1}×(n−1) + Close_t)/n`），并点明它等价于什么（如 α=1/n 的 EMA）。
   - 有阈值/交叉的写出触发条件（如 `fast(t) > slow(t) 且 fast(t−1) ≤ slow(t−1)`）。
   - 有 ATR/标准差/带宽的写出 `距离 = k × ATR(n)` 这类。
   - 有核函数、距离函数、平滑器或聚类距离时，写明本次固定实现，并说明它是否可以替换；可替换不等于结果可沿用。
   → 新报告使用 `explain.deep.equations[]`（公式名、表达式、符号清单和一句解释）；`deep.formula` 只兼容旧报告

2.5. **数学推导（默认折叠，给想深究的人）** — 在公式之外，用普通话解释"这公式为什么长这样"。
   - 从直觉/定义开始，不要从符号开始。例：SMMA 先说"上一根均线是旧共识，新价格是新信息，每根 K 线只吸收 1/n 的新旧差值"。
   - 再给 1-3 步紧凑推导，最后把参数的物理意义接回用户能懂的话。
   - 计算示例不能从输入直接跳到结果。至少写出“代入权重 → 分子与分母 → 归一化结果 → 阈值或交易条件”；每一步保留关键中间值，让读者可以拿计算器复核。
   - 这部分会在报告里折叠显示，不要把它写进 `formula` 里吓退普通用户。
   → 报告 `explain.deep.derivation`；优先同时提供 `deep.intuition`、`deep.variables[]` 和 `deep.example`

3. **它依赖哪种 market inefficiency（edge 的本质）** — 一句话说明策略成立所依赖的市场行为：
   动量延续 / 均值回归 / 波动率收缩后扩张 / 趋势自相关 / 超买超卖反转…。**这决定它在什么行情有效。**

4. **参数怎么影响** — 每个可调 input：调大/调小各自的代价与收益（灵敏度↔噪声、信号数↔胜率、滞后↔稳）。
   - **★ 必须挂靠到前面讲过的机制/公式的具体量**——用户只有先懂指标才懂参数。反例「Fast：快线长度」（脱离机制，看不懂）；正例「Fast＝公式里那条快线的 n，越小越贴价格、噪声越多；越大越钝、信号少而稳」。
   - 给紧凑的「调大 ↔ 调小换来什么」，别写成大段。
   - **指出关键参数**（真正决定成败的 1-2 个，如 TMA 的 Long Only），填进报告 `explain.keyParam`（报告会高亮标「关键」）。
   → 正文只取 1–3 个关键参数填 `explain.keyParams[]`（name + effect）

5. **有效场景 / 失效场景** — 它在哪类 regime 能赚、哪类会被反复打脸。用具体行情描述。
   → 报告 `explain.worksWhen` / `explain.failsWhen`

6. **假设与局限** — 它默认成立什么（如"趋势有持续性""波动率均值回复"），这些假设什么时候破。诚实点。

7. **与同类对比（可选）** — 和相近指标的关键差异（如 SMMA vs EMA vs SMA 的平滑差异；UT Bot 的 ATR 止损 vs 固定百分比止损）。

8. **★ 重绘 / 未来函数审计** — 见 SKILL.md J8：查 `lookahead_on` / 未确认收盘值 / 负 offset / `calc_on_every_tick`。
   → 报告 `explain.repaint`（clean/sanitized）+ `explain.repaintNote`

## 质量标准（自检）

- **写了真公式**，不是"它用了 XX 均线"这种糊弄话。
- **说明了 edge 的本质**（依赖哪种市场行为），不只罗列功能。
- **诚实讲局限与失效**，不吹成圣杯。
- 语言给**普通人**能懂：术语出现即用一句话解释。
- 文案用平实的陈述句，少用“它不是……而是……”“押注”“发言权”等修辞。先说输入、计算和输出，再说明交易规则与局限。

> 反例：只写"这是个趋势指标，金叉买死叉卖" = 太薄，不合格。
> 正例：写出 SMMA 递推式 + 说明策略依赖趋势延续 + 指出震荡里均线频繁交叉会反复止损 + 展示参数敏感性。
