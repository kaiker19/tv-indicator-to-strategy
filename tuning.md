# 有界参数研究

调参的目标不是找到一组最好看的数字，而是判断指标在明确参数空间里是否有可复核优势、结果是否形成参数高原、同一配置能否迁移到标准市场。

## 先确定执行预算

用户只要求解释、转换或验证时，运行一组可解释基线。用户要求研究参数、寻找更优配置或完整验证时，直接进入标准有界搜索，不再让用户选择“轻度”或“深化”模式。

- 合理网格不超过 30 组：完整覆盖。
- 更大网格：默认最多 30 次完整回测，先均匀粗搜，再对有效 Top-1 做邻域复核。
- 用户明确给出 `--budget`：按用户预算执行。

预算限制的是 TradingView 完整回测次数和 CDP 稳定性，不是 Agent 推理量。执行前仍需展示参数轴、范围、预算、开发市场、成本和验证市场。

## 输入审计

搜索前读取 Pine 源码中的全部 `input.*`，逐项归类：

- 核心机制：直接改变信号或状态；
- 风险与执行：止损、退出、仓位、确认时点；
- 固定：本轮保持不变；
- 派生：由其他输入计算，不独立搜索；
- 视觉：颜色、线宽、标签等，不进入回测；
- 死参数：声明存在但没有进入交易路径。

选择 1–3 个真正生效且能解释的轴。未扫描的核心输入必须在报告中列出原因；不能因为预算有限就假装它们不存在。

## 搜索覆盖合同

1. 固定源码身份、symbol、timeframe、成本和评估区间。
2. 每个轴先覆盖合理宽范围，包含至少一个边界或哨兵值。
3. 宽搜完成后，只在本次 Top-1 有效且形状不是孤立峰、噪声、样本不足或无优势时检查邻域。
4. 邻域至少覆盖 Top-1 左右的相邻值；若 Top-1 在边界，报告说明搜索范围可能截断结果。
5. 全部运行数受 `--budget` 硬限制；不能因结果不好而临时扩大未声明范围。

报告使用“本次参数空间 Top-1”或“当前有界搜索相对最好”，不能使用“全局最优”。

## 目标函数

- 默认 `risk_adjusted`，计算为净收益 ÷ 最大回撤；它不是 PF；
- 用户明确追求绝对收益时可用 `net_pnl`；
- 研究盈利质量时可用 `profit_factor`，但必须同时检查交易数；
- `alpha` 只在每个候选格的策略与 B&H 区间可比时有效；
- 研究低频胜率时使用 `win_rate_confidence`，不按原始胜率挑极少样本。

无论目标函数是什么，摘要都应保留策略收益、B&H、alpha、最大回撤、PF、交易数和基准来源。

## 命令形态

显式参数扫描：

```bash
node "$ENGINE" /tmp/strategy.pine \
  --symbol NASDAQ:QQQ --timeframe D \
  --scan "Length=7..21:7" \
  --scan "Factor=1..3:1" \
  --objective risk_adjusted \
  --commission 0.15 --slippage 2 --budget 9
```

标准优化与固定参数跨市场验证：

```bash
node "$ENGINE" /tmp/strategy.pine \
  --symbols NASDAQ:QQQ,AMEX:SPY,SSE:510300 --timeframe D \
  --optimize "Length=5..30:5" \
  --optimize "Factor=1..5:0.5" \
  --objective risk_adjusted \
  --commission 0.15 --slippage 2 --budget 30
```

引擎会在第一个 symbol 上优化，再将 Top-1 固定应用到其他 symbol。逐市场重新选择参数只能形成多个标的专属配置，不能合并成跨市场稳健结论。

## 基准合同

`alpha` 必须使用每个候选格同次读取的 Benchmarking B&H。若相同市场不同格的 B&H 明显漂移，表示加载区间或历史范围不一致：

1. 将比较状态标为 `incompatible`；
2. 停止按 alpha 排名；
3. 使用引擎给出的 `effectiveObjective` 保留风险诊断；
4. 统一范围后才重新做超额比较。

旧截图、预计算基准或其他运行的 B&H 不能替代当前候选格证据。

## 停止条件

以下任一条件出现时，不继续为了找赢家而扩参：

- 全部候选无交易或关键参数未进入策略；
- 全部有效候选无优势，形状为 `no_edge`；
- Top-1 是孤立峰，邻近值明显退化；
- 交易数过低，或 PF 异常但缺乏样本支持；
- 基准区间不可比较；
- 源码、策略、参数或页面身份不一致；
- 预算已经用完；
- 更大网格只重复同一结论。

停止扩参是“研究决策”。下一项若存在，应是能证伪机制的实验，例如比较原始语义与修正版、测试某个信号是否只适合作过滤器，或先统一回测区间；不能把“继续扩大参数”当作默认下一步。

## 多市场验证

完整公开案例固定展示 QQQ、SPY、510300，并保持：

- 同一源码摘要；
- 同一 Strategy Spec；
- 同一 Top-1 参数；
- 同一 timeframe；
- 同一手续费和滑点；
- 可解释的评估区间口径。

只有多个市场在同一合同下提供充分证据，才可以使用“跨市场稳健”。单市场通过时只能说“标的专属候选”，且还需要参数邻域支持。

## 报告要求

最终写明：

- 搜索模式、轴、范围、预算、实际运行数；
- 固定参数和未覆盖参数；
- 本次 Top-1 与邻域形状；
- 开发市场和三市场固定参数结果；
- 基准可比性；
- 停止原因；
- 一个研究决策或下一项可证伪实验。

扫描结束后必须恢复开发市场和 Top-1 参数，再读取一次 Strategy Tester 并保存 proof。没有恢复证明时，扫描中间值不能作为最终结论。
